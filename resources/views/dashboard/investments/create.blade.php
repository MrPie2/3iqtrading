@extends('layouts.app')
@section('title',$plan->Plan_Name)
@section('heading',$plan->Plan_Name)
@section('content')
<div class="row"><div class="col-lg-7"><div class="card table-card"><div class="card-body"><h3>{{ $plan->Plan_Name }}</h3><div class="text-muted mb-3">{{ $plan->Duration }} day term · {{ $plan->Percentage }}% illustrative rate</div><div class="mb-4">{!! $plan->Description !!}</div><div class="row"><div class="col-6"><span class="text-muted">Minimum</span><h5>{{ auth('investor')->user()->curAbbr }}{{ number_format((float)$plan->Minimum*(float)auth('investor')->user()->exchangerate,2) }}</h5></div><div class="col-6"><span class="text-muted">Maximum</span><h5>{{ auth('investor')->user()->curAbbr }}{{ number_format((float)$plan->Maximum*(float)auth('investor')->user()->exchangerate,2) }}</h5></div></div></div></div>
<div class="col-lg-5"><div class="card table-card"><div class="card-body"><h5>Start investment</h5><form id="investForm">@csrf<input type="hidden" name="plan_type" value="{{ $plan->Plan_Name }}"><input type="hidden" name="percentage" value="{{ $plan->Percentage }}"><div class="form-group"><label>Amount</label><input name="amount" type="number" step="0.01" class="form-control" min="{{ $plan->Minimum }}" max="{{ $plan->Maximum }}" required></div><div class="form-group"><label>Duration (days)</label><input name="duration" type="number" class="form-control" value="{{ $plan->Duration }}" min="1" required></div><button class="btn btn-brand btn-block">Place investment</button></form><div id="msg" class="small mt-3"></div></div></div></div></div>
@endsection
@push('scripts')<script>document.getElementById('investForm').addEventListener('submit',async e=>{e.preventDefault();try{const d=await api('{{ route('investments.store') }}',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams(new FormData(e.target))});document.getElementById('msg').innerHTML='<span class="text-success">'+d.message+'</span>';setTimeout(()=>location.href=d.redirect,700)}catch(x){document.getElementById('msg').innerHTML='<span class="text-danger">'+x.message+'</span>'}})</script>@endpush