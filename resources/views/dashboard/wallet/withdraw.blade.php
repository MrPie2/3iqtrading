@extends('layouts.dashboard')
@section('title','Withdraw')
@section('heading','Withdraw')
@section('content')
<div class="row"><div class="col-lg-5"><div class="card metric mb-4"><div class="card-body"><div class="text-muted">Portfolio available</div><div class="value">{{ $investor->curAbbr }}{{ number_format($investor->Fin_Asset*(float)$investor->exchangerate,2) }}</div></div></div><div class="card table-card"><div class="card-body"><h5>Request withdrawal</h5><form id="withdrawForm">@csrf<div class="form-group"><label>Amount</label><input name="amount" type="number" step="0.01" class="form-control" required></div><div class="form-group"><label>Destination</label><input name="pay_to" class="form-control" placeholder="Wallet address / account reference" required></div><div class="form-group"><label>Asset / method</label><input name="bop" class="form-control" placeholder="e.g. Bitcoin"></div><div class="form-group"><label>Network</label><input name="network" class="form-control"></div><button class="btn btn-brand btn-block">Submit withdrawal</button></form><div id="msg" class="small mt-3"></div></div></div></div><div class="col-lg-7"><div class="card table-card"><div class="card-body"><h5>Withdrawal history</h5><div class="table-responsive"><table class="table"><thead><tr><th>Date</th><th>Amount</th><th>Destination</th><th>Status</th></tr></thead><tbody>@forelse($withdrawals as $w)<tr><td>{{ $w->Date }}</td><td>{{ $investor->curAbbr }}{{ number_format($w->Amount_Withdrawn*(float)$investor->exchangerate,2) }}</td><td>{{ $w->Destination }}</td><td>{{ $w->Status==0?'Pending':($w->Status==1?'Approved':'Completed') }}</td></tr>@empty<tr><td colspan="4">No withdrawals.</td></tr>@endforelse</tbody></table></div></div></div></div></div>
@endsection
@push('scripts')<script>document.getElementById('withdrawForm').onsubmit=async e=>{e.preventDefault();try{const d=await api('{{ route('withdraw.store') }}',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams(new FormData(e.target))});document.getElementById('msg').innerHTML='<span class="text-success">'+d.message+'</span>';setTimeout(()=>location.reload(),600)}catch(x){document.getElementById('msg').innerHTML='<span class="text-danger">'+x.message+'</span>'}}</script>@endpush