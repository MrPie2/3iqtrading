@extends('layouts.app', ['title' => 'Shares — 3IQ Trading'])
@section('content')
<section class="page-hero"><div class="container"><div class="eyebrow">Share opportunities</div><h1 class="mt-2">Explore share-based investment options.</h1><p class="mt-3 mb-0">Use this page for approved share products, ownership information, pricing and risk disclosures.</p></div></section>
<section class="section"><div class="container"><div class="row g-4">@foreach($plans as $plan)<div class="col-md-6 col-lg-4"><div class="plan-card {{ $plan->featured ? 'featured' : '' }}">@if($plan->featured)<span class="plan-badge">Popular</span>@endif<div class="icon-box"><i class="bi bi-pie-chart"></i></div><h4 class="fw-bold">{{ $plan->name }}</h4><p class="text-secondary">{{ $plan->description }}</p><div class="price">${{ number_format($plan->minimum_amount,0) }}</div><div class="small text-secondary">Minimum amount</div><ul class="plan-list">@foreach(($plan->features ?? []) as $feature)<li><i class="bi bi-check-circle-fill"></i>{{ $feature }}</li>@endforeach</ul><a href="{{ route('register') }}" class="btn btn-primary rounded-pill w-100">Continue</a></div></div>@endforeach</div></div></section>
@endsection
